spring:
  application:
    name: zomato-clone

  datasource:
    url: jdbc:postgresql://localhost:5432/zomatoclone
    username: postgres
    password: postgres

  jpa:
    hibernate:
      ddl-auto: validate
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration
